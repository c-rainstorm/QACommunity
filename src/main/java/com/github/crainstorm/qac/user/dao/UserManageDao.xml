<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.crainstorm.qac.user.dao.UserManageDao">

    <select id="checkUserLogin" parameterType="com.github.crainstorm.qac.pub.entity.UserLogin" resultType="int">
        SELECT COUNT(*)
        FROM user
        WHERE email = #{email} AND password = #{password};
    </select>

    <select id="checkUserEmail" resultType="int">
        SELECT COUNT(*)
        FROM user
        WHERE email = #{email};
    </select>

    <select id="getUserDetails" parameterType="com.github.crainstorm.qac.pub.entity.UserLogin"
            resultType="com.github.crainstorm.qac.pub.entity.User">
        SELECT id, name, email, create_date, sex, short_intro, intro, college, major, avatar, prestige, browse_num, status
        FROM user
        WHERE id = #{id};
    </select>

    <select id="getUserBriefInfo" parameterType="com.github.crainstorm.qac.pub.entity.UserLogin"
            resultType="com.github.crainstorm.qac.pub.entity.User">
        SELECT id, name, avatar, short_intro
        FROM user
        WHERE id = #{id};
    </select>


    <insert id="addUser" parameterType="com.github.crainstorm.qac.pub.entity.User">
        INSERT INTO `user` (name, email, password, create_date)
        VALUES (#{name},#{email},#{password},curdate());
    </insert>

    <select id="getNotice" resultType="com.github.crainstorm.qac.pub.entity.Notice">
        SELECT id,user_id,content,datetime
        FROM notice WHERE id = #{id};
    </select>
</mapper>